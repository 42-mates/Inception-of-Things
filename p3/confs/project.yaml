apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: development
  labels:
    app: iot-argocd
spec:
  description: ArgoCD Project to deploy app wil42
  sourceRepos:
    - "https://github.com/brgman/*"
  destinations:
    - namespace: dev
      server: https://kubernetes.default.svc
  orphanedResources:
    warn: false